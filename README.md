# document_layout_detector

[!] PublayNet's pretrained model cannot be downloaded... So this project cannot be realized.

## How to use

# Building Container

```bash
docker build -t layoutparser-cpu docker/
#DOCKER_BUILDKIT=0 docker build -t layoutparser-cpu docker/
```

# Executing script 

```bash
python main.py
```

---
